const routes = {
  createMessage: '/api/message',
  createThread: '/api/message-thread',
  broadcastMessage: '/api/message-broadcast',
  getMessages: '/api/messages/:propertyId',
  getMessagesThread: '/api/messages-thread/:threadId',
  getThreads: '/api/message-threads',
}

export default routes
